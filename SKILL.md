---
name: repo-atlas
description: 从真实仓库生成可追溯源码的项目或模块分析，覆盖架构、关键业务链、数据、部署、外部依赖与未确认项，并交付可离线探索的交互 HTML。适用于接手复杂项目、梳理模块边界或制作架构图谱；不替代专项代码审计、缺陷修复或新系统设计。
---

# 项目模块图谱

把“项目解决什么问题、由什么组成、谁调用谁、关键事实如何流动、哪里仍不确定”整理成可验证、可探索的离线报告。默认使用本技能的模板，保留全文检索、关系图导航、缩放拖动、源码摘录、覆盖矩阵，以及 SVG / Mermaid 导出。

## 范围与读者

从会话和当前目录确定项目路径、分析范围、主要读者与输出位置。未指定时，分析当前仓库，面向接手项目的研发人员，输出到工作区独立文档目录。单模块请求只追踪必要上下游；多个相关仓库可以统一纳入，但必须区分仓库、部署单元和业务分组。

先读取最近的 `AGENTS.md`、`CLAUDE.md`、README、项目记忆和交接资料，记录影响本次工作的约束。没有项目记忆或 Git 基线就明确说明。创建分析文档不授权修改业务代码、运行迁移、连接生产、上传文件或写长期记忆。

开始深入前先回答：读者最需要用报告完成什么判断；哪些业务链或模块优先；哪些环境事实本次无法核验。不要把源码目录结构直接当作架构，也不要为追求图表数量扩大范围。

## 建立证据模型

1. 记录实际分支、提交与未提交变化，枚举入口、业务编排、数据访问、模型、配置、任务、部署描述、外部适配器和测试。文件规模可由工具统计，职责必须通过阅读判断。
2. 建立与范围相称的覆盖账本。常见维度包括入口与角色、认证授权、主业务链、状态与数据、异步任务、外部副作用、失败恢复、一致性与并发、配置、部署运行、可观测性和测试。只保留相关维度，并明确 `covered`、`partial`、`unknown` 或 `not_applicable`。
3. 以用户动作或业务事件为线索，追踪入口 -> 校验与权限 -> 编排 -> 数据读写 -> 外部副作用 -> 下游读取。对改变业务事实的动作确认事务边界、重复处理、并发、部分成功、重试、补偿和状态迁移；没有实现就如实记录。
4. 为重要关系保存小段证据：真实路径、稳定文本锚点和足够的上下文。调用点与被调用实现至少交叉核对一次；不能用类名、字段名、注释或 DDL 的存在推断主链行为。
5. 区分当前值、确认计划、执行结果、历史快照、审计记录和兼容路径。对持久化对象确认关联键、基数、唯一性、写入者和消费者。字段名含 `fk` 不代表物理外键，表名被引用不代表已投产或存在业务写入。
6. 区分已确认事实、基于证据的推断和未确认项。对会影响接手、变更或运行判断的结论，使用 `findings` 记录类型、可信状态、影响和最短验证路径；不要把普通代码观察包装成风险清单。

详细的覆盖取舍和证据门槛见 [analysis-guide.md](references/analysis-guide.md)。分析跨越多个模块、包含数据写入或存在明显未知时，必须读取该文件。

## 选择视图

先给出一张能说明系统边界和主路径的总览，再按每条重要业务链和读者问题拆图。复杂项目不能只交付一张总链路图：每条关键链路必须在 `chains` 中登记，并把入口与权限、校验与编排、数据读写、外部副作用、结果与下游、失败恢复拆成阶段，绑定对应源码证据和待确认项。每条核心链路至少交付一张专属时序图；包含状态迁移、持久化关系、异步任务或运行边界时，再分别补充状态、数据、一致性、失败恢复和部署视图。

如果仓库存在多条用户旅程、事件消费链、批处理链或补偿链，应按链路分别建图，不把互不相同的触发、状态和失败语义压进一张图。图表数量由链路数量和读者问题决定；少于三张图的复杂项目应在覆盖矩阵中明确说明原因和缺口。

- 每张图只表达一种主关系；边必须带有调用、读写、发布、消费或返回等明确动词。
- 组织归属不暗示运行调用；源码目录不暗示独立部署；配置默认值不等于线上有效值。
- 状态图来自真实枚举和迁移逻辑。多个状态机分开；未连通或不可达状态说明来源。
- 数据图至少能回到“左对象.键 <-> 右对象.关联属性”；基数必须有实现或约束证据。
- 总览使用业务可读名称，真实接口、表名、状态码和类名放在细节视图、别名或证据弹窗。
- 长时序图保持可读字号；手机可提供同义纵向图，但不能删掉影响理解的条件。

第一屏应让读者快速看到项目定位、分析基线、核心关系和证据规模。重要结论进入发现项，覆盖缺口进入覆盖矩阵，不把它们藏在长段说明中。

## 生成报告

生成前完整读取 [manifest-format.md](references/manifest-format.md)，在项目文档目录创建 UTF-8 `atlas.json`。业务结论、覆盖状态与 Mermaid 内容由读过源码的助手编写；生成器只校验结构、提取摘录、统计文件和读取 Git 基线，不会自动理解业务。

```text
node <skill-dir>/scripts/build.mjs <project-docs>/atlas.json
```

要求 Node.js 18+。`workspace` 相对清单文件定位，`output` 必须位于工作区内，多仓路径均相对 `workspace`。无 Git 目录可省略 `repositories`。生成的 HTML 内嵌 Mermaid 10.9.3、Lucide 0.468.0、样式、图表和证据，默认离线打开，不启动项目应用，也不安装其依赖。

项目专属内容只放清单，不要修改模板来迎合单个项目。不要覆盖已有报告，除非用户要求更新或输出明确指向该文件；此时才使用 `--replace`。

只摘取说明关系所需的代码。配置只展示明确挑选且可公开的值，不把凭据、个人数据、生产样本或整个环境文件打包进报告。`redact` 只能作为最后一道字面遮盖，写出前仍需人工检查清单与 HTML。

## 快照与增量更新

不要每次代码变化都重新读取整个仓库或把完整源码放入模型上下文。首次报告生成后，在工作区保存本地快照：

```text
node <skill-dir>/scripts/snapshot.mjs <project-docs>/atlas.json
```

之后只在本地计算文件哈希、Git 状态和证据反向索引（证据文件每次重新核对哈希，未引用文件可复用未变化的文件系统元数据）：

```text
node <skill-dir>/scripts/delta.mjs <project-docs>/atlas.json
node <skill-dir>/scripts/refresh.mjs <project-docs>/atlas.json --delta .repo-atlas/delta.json
node <skill-dir>/scripts/context.mjs <project-docs>/atlas.json --changed-only
```

增量结果包含新增、修改、删除、重命名、受影响模块、失效证据、复用/重算证据数量和是否建议全量重分析。`delta.json` 只保存差异摘要，不重复嵌入当前完整快照；需要同时落盘候选快照时可加 `--snapshot-output .repo-atlas/current-snapshot.json`。源文件被删除或证据锚点消失时，快照记录 `resolved: false` 和原因，不伪造旧摘录。

刷新器输出 `atlas.next.json`，不覆盖分析者维护的基线清单；只有复核受影响源码后才接受候选清单并重新生成快照。清单本身发生变化、公共基础设施大范围变化或变更超过阈值时，候选项统一标记为待复核并建议全量重分析。模型上下文只带变更 hunk、受影响模块旧摘要、相关证据摘录和必要的一跳调用关系，未变化模块直接复用。

`context.mjs` 生成 `.repo-atlas/context.json`，默认只携带 delta 中的变更文件、Git hunk、受影响链路/阶段、旧新证据摘录和一跳上下游关系；使用 `--stale-only` 只处理失效证据，使用 `--previous-manifest` 显式加入旧清单摘要。该文件是模型复核输入，不替代完整 `atlas.json` 或快照。

默认影响规则：普通实现变更影响所在模块和直接证据；路由、数据模型、锁文件、部署或公共配置变化扩大到相关入口、数据关系和运行视图；至少 10 个基线文件时变更超过约 20%、变更达到 20 个文件，或命中公共基础设施时建议全量重分析。`.repo-atlas` 快照目录默认不进入文件索引，避免工具产物污染基线。快照和差异文件只保存在本地，不上传仓库。

## 验证与交付

生成器首先校验路径、证据锚点、重复 ID、模块引用、发现项和覆盖状态。可用浏览器时运行：

```text
node <skill-dir>/scripts/verify.mjs <report.html> --playwright <installed-playwright/index.mjs>
```

验证脚本在断网 Chromium 中遍历实际图表和移动变体，检查渲染、页面错误、缩放拖动、证据弹窗、全文检索、索引过滤、导出、导航和移动端溢出，并写出截图和结果。Playwright 可通过显式路径、`REPO_ATLAS_PLAYWRIGHT_PATH` 或已安装包解析；脚本不下载浏览器或依赖。缺少环境时优先使用现有浏览器能力，无法执行就报告未验证项。

人工检查总览、最密集数据图、最长时序图、发现项与覆盖矩阵，以及桌面和手机截图。重点检查字体过小、连线压字、裁切、空白失衡、术语不一致和结论与证据不匹配。浏览器通过只证明报告可用，不能替代对业务结论的复核。

交付可直接打开的 HTML、可维护的 `atlas.json`，以及需要持续维护时的 `.repo-atlas/snapshot.json` 与 `delta.json`；说明覆盖范围、实际验证、最重要的已确认结论、未确认项和增量复核范围。不要为套模板新增迁移方案、专项风险审计或规划需求。需要复制提示词到其他工具时，再提供 [可复制提示词.md](references/可复制提示词.md)。
